# stringtie / nextflow / local 占位 meta.yaml
# ---------------------------------------------------------------------------
# 当需要自维护的 Nextflow process（nf-core 缺失或定制场景）时：
#   1) 把 id/version/source_reference/inputs/outputs 补全
#   2) 在同级目录编写 <PROCESS>.nf 文件
# ---------------------------------------------------------------------------

id: stringtie_nextflow_local
version: "0.0.0-placeholder"
software: stringtie
type: nextflow_local
source_type: custom

summary: "Placeholder for custom stringtie Nextflow modules (populate when needed)."

agent_guidance:
  when_to_use: "nf-core 子模块缺失，或需要本地定制 stringtie process 时使用。"
  recommendation_priority: "Low（nf-core 优先）。"
  enabled: false  # 占位：未补 main.nf / inputs/outputs 前，Agent 不得路由到此实现。

software_versions:
  status: placeholder
  note: "本地 Nextflow 自维护模块尚未填充版本信息；实际启用时建议显式 pin bioconda::stringtie=3.0.3 或继承 native 容器。"

inputs: {}
outputs: {}
execution:
  mode: nextflow_include
  include_statement: ""
