# orfanage / snakemake / snakemake-wrappers 占位说明
# ---------------------------------------------------------------------------
# snakemake-wrappers 官方无 bio/orfanage wrapper（2026-09 核对 404）。
# Snakemake 场景请使用 ../local/orfanage.smk（迁移自 flrnaseq orfrange_archive）。
# ---------------------------------------------------------------------------

id: orfanage_snakemake_wrappers
version: ""
software: orfanage
type: snakemake_wrappers
source_type: official

summary: "snakemake-wrappers 官方暂无 bio/orfanage wrapper（占位说明层）。"

agent_guidance:
  when_to_use: "官方 wrapper 缺失；Snakemake 场景请走 ../local/。"
  recommendation_priority: "Low（官方不存在，占位）。"

software_versions:
  orfanage: "官方无"
  wrapper_tag: "v3.13.0"
  source: "（占位）"
  note: "若未来 snakemake-wrappers 新增 bio/orfanage，刷新本文件。"

execution:
  mode: snakemake_rule
  wrapper_template: ""
  rule_example: |
    # 官方 wrapper 不存在；使用 snakemake/local/orfanage.smk：
    include: "skills/orfanage/snakemake/local/orfanage.smk"
