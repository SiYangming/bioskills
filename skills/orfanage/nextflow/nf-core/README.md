# orfanage / nextflow / nf-core（占位）

> **本目录仅为说明层：nf-core/modules 官方没有 orfanage 模块，无法 `nf-core modules install`。**
> Nextflow 场景请使用 `../local/` 自实现 process，或直接降级到 `native`。

- 参考命令（未来有模块时）：`nf modules install nf-core orfanage`
- 官方目录：https://github.com/nf-core/modules/tree/master/modules/nf-core/orfanage（当前 404）
