# samtools / nextflow / local 占位 meta.yaml
# ---------------------------------------------------------------------------
# 当需要自维护的 Nextflow process（nf-core 缺失或定制场景）时：
#   1) 把 id/version/source_reference/inputs/outputs 补全
#   2) 在同级目录编写 <PROCESS>.nf 文件
# ---------------------------------------------------------------------------

id: samtools_nextflow_local
version: ""
software: samtools
type: nextflow_local
source_type: custom

summary: "Placeholder for custom samtools Nextflow modules (populate when needed)."

agent_guidance:
  when_to_use: "nf-core 子模块缺失，或需要本地定制 process 时使用。"
  recommendation_priority: "Low（nf-core 优先）。"

inputs: {}
outputs: {}
execution:
  mode: nextflow_include
  include_statement: ""
