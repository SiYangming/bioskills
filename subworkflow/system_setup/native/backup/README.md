# system_setup 源脚本备份（未修改）

本目录存放教学素材**原样备份**，不参与 `native/main.py` 编排执行。

| 子目录 | 来源 | 说明 |
|---|---|---|
| `01.CentOS_System_Configuration/` | `training_pipelines/01.CentOS_System_Configuration/` | CentOS 8 章两份 `.sh`，字节级原样 |
| `_unused/bash_scripts/` | `training_pipelines/_unused/bash_scripts/` | CentOS 6 / 历史脚本；**需从本机 Desktop 同步**（云端环境无该路径） |

编排入口仍为上级的 `main.py` + `system_setup_stages.sh`（参数化改写版）。改系统请用编排器；本目录仅存档对照。
